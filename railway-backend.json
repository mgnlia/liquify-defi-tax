{
  "name": "liquify-defi-tax-api",
  "source": {
    "type": "github",
    "repo": "mgnlia/liquify-defi-tax",
    "branch": "main"
  },
  "build": {
    "dockerfilePath": "backend/Dockerfile"
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health"
  }
}
